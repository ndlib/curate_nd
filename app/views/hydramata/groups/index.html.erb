<h2>Group Administration &nbsp;
  <% if can? :create, Hydramata::Group %>
      <%= button_for_create_new_group %>
  <% end %>
</h2>
<br/>
<% if @response.response["numFound"] > 0 %>
  <%= paginate @response, :entry_name=>'item', :outer_window => 2, :theme => 'blacklight' %>
  <%= render_document_index %>
<% else %>
  <div class="col-sm-12 main-header">
    <p>You have no groups yet.</p>
  </div>
<% end %>
